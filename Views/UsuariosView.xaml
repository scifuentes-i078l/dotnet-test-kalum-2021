<Window x:Class="Kalum2021.Views.UsuariosView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kalum2021"
        mc:Ignorable="d"
        Title="Lista de usuarios" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <DataGrid SelectedItem ="{Binding Seleccionado}" Grid.Row="0" Grid.Column="0" ItemsSource= "{Binding usuarios}" AutoGenerateColumns = "true"></DataGrid>
        <StackPanel Grid.Row="1" Grid.Column="0" >
            <Button  Content="Nuevo" Command="{Binding Instancia}" CommandParameter="Nuevo"/>
            <Button  Content="Eliminar" Command="{Binding Instancia}" CommandParameter="Eliminar"/>
        </StackPanel>
        

    </Grid>
</Window>