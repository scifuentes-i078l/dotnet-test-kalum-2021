<Window x:Class="Kalum2021.Views.RolView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kalum2021"
        mc:Ignorable="d"
        Title="Rol" Height="450" Width="800" WindowStartupLocation="CenterScreen" Name="FormularioUsuario">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Rol" />
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Rol}"/>
        <StackPanel Grid.Row="1" Grid.Column="0"  Orientation="Horizontal" >
            <Button  Content="Guardar" Command="{Binding Instancia}" CommandParameter="Guardar"/>
            <Button  Content="Cancelar" Command="{Binding Instancia}" CommandParameter="Cancelar"/>
        </StackPanel>
        

    </Grid>
</Window>